<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Gui.ViewModels;assembly=Gui"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Gui.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="Gui">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    
    <Grid ColumnDefinitions="100, 2*, 1*">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" ></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Name}" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button Grid.Column="2" Command="{Binding DoClickCommand}">Say hello</Button>
        <TextBlock Grid.Row="2" Grid.ColumnSpan="3" Text="{Binding Greeting}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
    
</Window>
